const books = require('./books');

// -------------------------------------------------------------------------------------------------------------------------------------------
// 3.1: create an array to store authors of first 2 books
let bookAuthors = [...books[0].author, ...books[1].author];
console.log(bookAuthors);
// -------------------------------------------------------------------------------------------------------------------------------------------

// -------------------------------------------------------------------------------------------------------------------------------------------
// 3.2: write function called spellWord, single string as argument
// log to console each letter separated by space
function spellWord(str) {
  console.log(...str);
}
spellWord('JavaScript');

// -------------------------------------------------------------------------------------------------------------------------------------------
